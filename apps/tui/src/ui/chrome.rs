//! TUI chrome: navigator, status bar, new-agent sheet.
//!
//! Pure rendering. Takes a view model, emits a frame. No I/O, no persistent
//! state.

// TODO(P1): navigator widget. Shape (Option A vs B) decided during P1
// implementation per `docs/architecture.md` "Navigator layout — OPEN".
// TODO(P1): status bar (one row, prefix-key state indicator).
// TODO(P1): new-agent sheet (host picker + cwd picker + optional prompt).
