# Testing

How we test codemux end-to-end. Short by design — update as we ship.

## Stack

Two test shapes, two stacks. They share `insta` and `pretty_assertions`; nothing else.

**TUI tests** (`apps/tui/tests/`)

| Tool | Role |
|---|---|
| `ratatui::backend::TestBackend` | In-process render-layer harness for the fast tier |
| `portable-pty` | Spawn the real `codemux` binary in PTY tests (already a prod dep) |
| `vt100` | Parse the master-side byte stream into a cell grid (already transitive via `tui-term`) |
| `insta` (+ `cargo-insta`) | Snapshot diffing of `Buffer` / `vt100::Screen` |
| `serial_test` | `#[serial]` on PTY tests so terminal-size negotiation doesn't race |
| `pretty_assertions` | Readable diffs on buffer asserts |

**Daemon tests** (`apps/daemon/tests/`)

| Tool | Role |
|---|---|
| `std::process::Command` | Spawn the daemon binary as a regular subprocess (no PTY) |
| `crates/wire` (as a client) | Drive the daemon via its own protocol — same surface real codemux uses |
| `insta` | Snapshot diffing of decoded protocol responses |
| `pretty_assertions` | Readable diffs |

We are **not** adopting a Playwright-style framework. Microsoft's `tui-test` is the only mature option in that category and it would drag a Node toolchain into a pure-Rust repo. Termwright (Rust-native) is too young (15 stars, v0.2). Skipped after evaluation: `expectrl`, `rexpect`, `assert_cmd`, `vhs`, `ratatui-testlib`. Re-evaluate Termwright in ~12 months.

## Layout

Each app owns its own E2E suite. The TUI and daemon share **no test infrastructure** — only the real `crates/wire` protocol crate, which is already part of the architecture.

```
apps/tui/
├── Cargo.toml                       # [[bin]] fake_agent gated by test-fakes feature
└── tests/
    ├── common/mod.rs                # PTY harness: spawn_codemux, send_keys, screen_eventually
    ├── bin/fake_agent.rs            # stub agent — only built with --features test-fakes
    ├── render_*.rs                  # fast tier (TestBackend), runs on every just check
    └── pty_*.rs                     # slow tier, #[ignore] by default

apps/daemon/
└── tests/
    ├── common/mod.rs                # protocol harness: spawn_codemuxd, wire client wiring
    └── proto_*.rs                   # daemon scenarios, #[ignore] by default
```

`fake_agent` lives in `apps/tui/tests/bin/fake_agent.rs` and is wired into `apps/tui/Cargo.toml` as:

```toml
[[bin]]
name = "fake_agent"
path = "tests/bin/fake_agent.rs"
required-features = ["test-fakes"]

[features]
test-fakes = []
```

`cargo build --release` does not build it. `cargo test --features test-fakes` does. Tests resolve the path via `env!("CARGO_BIN_EXE_fake_agent")`.

A `just check-e2e` recipe wraps `cargo test --features test-fakes -- --ignored` so day-to-day flow stays `just check`.

**No `crates/e2e/` workspace member.** Earlier drafts proposed one; daemon E2E doesn't share infrastructure with TUI E2E (different spawn mechanism, different observation surface, different assertions), so a unified test crate would be false coupling.

**Cross-cutting tests** (TUI → daemon → fake agent) live in `apps/tui/tests/` because the TUI is the driving end. The PTY harness gains a small `spawn_codemuxd()` helper alongside `spawn_codemux()` — ~10 lines around `std::process::Command`, no separate crate needed.

## Invariants

- TUI deps stay out of `crates/session`, `crates/shared-kernel`, `crates/wire`, `crates/codemuxd-bootstrap`. PTY/vt100 deps are confined to `apps/tui`'s `[dev-dependencies]`.
- Snapshot review goes through `cargo insta review`. No hand-edited `.snap` files.
- Frame stability has exactly one rule: poll `vt100::Screen` until the assertion holds or a timeout fires; on timeout, print the actual screen. Defined once in `apps/tui/tests/common/mod.rs::screen_eventually` and used everywhere. Never `sleep()` in tests.
- A `CODEMUX_AGENT_BIN` env var is the single indirection point for swapping the spawned agent in tests. Production codepath defaults to `claude`.
- Daemon tests speak `crates/wire` directly. They do not parse text logs, screen output, or anything other than the real protocol.

## Roadmap

### T0 — Fast-tier scaffolding (TUI)

- Add `insta`, `pretty_assertions` as dev-deps on `apps/tui`.
- First snapshot test: empty boot screen renders the expected chrome.
- Wire `cargo insta review` into the dev workflow.

**Ship test**: `just check` runs at least one snapshot test that fails meaningfully when chrome layout changes.

### T1 — Fast-tier coverage of core TUI flows

- Spawn modal: host/path zones, autocomplete, host-badge rendering.
- Navigator: agent rows, status dots, focus cycling.
- Help screen: keymap rendering stays in sync with the `Bindings` POD.

The render helpers were already test-reachable from `runtime.rs`'s existing `mod tests` block — no `Runtime`-constructibility refactor was needed. Tests construct chrome state directly (`Bindings::default`, `failed_agent` helper, etc.) and drive the same render fns the production frame loop calls.

**Ship test**: a chrome regression in any of the three views above is caught before commit.

### T2 — PTY harness + fake_agent — **shipped**

- `apps/tui/tests/common/mod.rs` exports `spawn_codemux`, `send_keys`, `screen_eventually`. Master-side bytes are pumped on a background thread into a `Mutex<Vec<u8>>`; `screen_eventually` drains the buffer into a `vt100::Parser` and re-checks the predicate every 5 ms until the deadline. On timeout it panics with the rendered screen contents inline.
- `apps/tui/tests/bin/fake_agent.rs` + the `test-fakes` feature. The fake writes `FAKE_AGENT_READY> ` to stdout, flushes, and discards stdin to EOF. No external deps.
- Agent-binary plumbing landed as a clap-resolved `--agent-bin` flag (with `env = "CODEMUX_AGENT_BIN"`, default `"claude"`). Production wires it into a `BinaryAgentSpawner` in `crates/session`; tests set the env var on the codemux subprocess and let the same spawner invoke the fake. **No** `FakeAgentSpawner` type — production code never reaches for `std::env::var`. Earlier draft of this plan referred to "an env var read at the spawn site"; the shipped seam is a clap-bound flag with env fallback, which keeps test wiring inside the existing CLI surface.
- Smoke test at `apps/tui/tests/pty_smoke.rs`: `fake_agent_prompt_renders` boots codemux in an 80x24 PTY against the fake and asserts the prompt makes it onto the rendered grid. Codemux's initial agent spawns automatically on boot (`runtime::run` calls `spawn_local_agent` up-front) so no keystrokes are needed.
- Slow-tier tests are double-gated: `#![cfg(feature = "test-fakes")]` keeps them out of the default build (the harness reaches for `env!("CARGO_BIN_EXE_fake_agent")` which only exists with the feature); `#[ignore = "..."]` keeps them out of `cargo test` even when the feature is on. `just check` stays fast.
- `just test-e2e`, `just test-all`, and `just check-e2e` are wired in `justfile` and use `--features codemux-tui/test-fakes` at the workspace test-runner level.

**Ship test — passing**: 20 consecutive `just test-e2e` runs all green (no flakes observed).

### T3 — TUI regression coverage

Slow-tier TUI tests that drive a real `codemux` PTY through keystrokes and assert on the rendered grid. The AC coverage matrix below is the source of truth for which scenarios land here; representative areas:

- Spawn-modal flows (host/path zones, autocomplete, drilldown, cancel).
- Agent lifecycle (spawn → focus cycle → kill → reap → auto-exit).
- Scrollback (enter, snap-to-live on type/paste, per-agent offset preservation).
- Mouse (click-to-focus, drag-to-reorder; selection/OSC 52 deferred — see notes in the matrix).

Bug-driven tests still land here as regressions surface; the matrix tracks the planned-but-not-yet-shipped surface so it doesn't get forgotten.

### T4 — Daemon E2E

- `apps/daemon/tests/common/mod.rs` with `spawn_codemuxd` and a wire-client wrapper.
- Cover the protocol surface that already exists; AC coverage matrix below enumerates daemon-tier scenarios (snapshot-on-attach, `setsid -f` survival, stale-daemon redeploy, eventual reattach across TUI restart).
- Daemon-only tests live in `apps/daemon/tests/`. Cross-cutting tests (codemux + codemuxd together) live in `apps/tui/tests/` and call `spawn_codemuxd` from the TUI harness.

**Ship test**: an E2E test that boots `codemuxd`, connects from `codemux` over wire, spawns a fake agent through the full path, and asserts the rendered output.

## Open decisions

- Fast-tier reach: render-only (gitui-style) vs full runtime with injected key channel (helix-style). Lean helix-style — more upfront work, far more useful coverage. Lock in during T1.
- vhs as documentation-grade goldens for `docs/`: separate axis from correctness testing. Skip until there's a screenshots pipeline.
- Real sshd vs fake-ssh stub for T3 SSH coverage. Stub-first; revisit if real protocol bugs slip through.
- Re-evaluate Termwright at ~v1.0 if it gets there. If it stabilizes with a real maintainer base, the PTY harness is a candidate to replace.

## Cross-linking workflow

Every AC in `docs/003--acceptance-criteria.md` has a row in the matrix below. When a test for an AC ships, two doc updates land in the same commit:

1. In this file, flip the matrix row's status from `planned` to `shipped`. If the test name diverges from the planned one, update it.
2. In `docs/003--acceptance-criteria.md`, under the corresponding `### AC-NNN:` heading, append a `**Tests:**` block immediately after `**Failure modes:**`:

   ```
   **Tests:**
   - `apps/tui/tests/<file>::<test_name>` — one-line note on what aspect of the AC it covers.
   ```

   List multiple tests as separate bullets when one AC needs more than one.

The bidirectional link lets a future reader walk from any AC to its tests and from any test back to the AC it pins. AC-001 is the seed example.

Test names in the matrix are planned, not promised. They're guesses about file layout and naming; the binding contract is the AC, not the path. Update freely as tests land.

## AC coverage matrix

Status values: `shipped` (test exists and passes the AC's main behavior), `partial` (existing tests pin some `**Then:**` clauses but not all — the AC's `**Tests:**` block lists what's covered and what's still uncovered), `planned` (named here, not yet written), `blocked` (infrastructure to even write the test does not exist yet — e.g. mouse-event encoding in the harness, an opener seam), `deferred` (architectural prerequisite missing — e.g. AD-7 persistence for AC-028).

The "Test" column points at an existing test path when one exists (`partial` and `shipped` rows) and at the planned-but-not-yet-written name otherwise. For ACs with multiple existing tests, see the AC's own `**Tests:**` block in `docs/003--acceptance-criteria.md` for the full list.

| AC | Title | Tier | Test | Status |
|---|---|---|---|---|
| AC-001 | Spawn the initial agent at launch | T2 | `apps/tui/tests/pty_smoke.rs::fake_agent_prompt_renders` | shipped |
| AC-002 | Spawn a local agent in the scratch directory | T3 | `apps/tui/tests/pty_spawn_action.rs::enter_in_empty_modal_spawns_second_agent_in_scratch_dir` (+ `spawn.rs::empty_path_with_no_selection_emits_spawn_scratch` and scratch-resolution unit tests) | shipped |
| AC-003 | Spawn a remote agent over SSH (cold-start bootstrap) | T4 | `crates/codemuxd-bootstrap/src/lib.rs::prepare_remote_happy_path_on_fresh_host` (+ stage-by-stage tests) | partial |
| AC-004 | Path-zone wildmenu autocompletes against the focused host | T3 | `apps/tui/src/spawn.rs::tab_in_path_zone_applies_highlighted_candidate`, `remote_completions_*`, `host_completions_*` | partial |
| AC-005 | Quick-switch to precise mode by typing `~` or `/` | T3 | `apps/tui/src/spawn.rs::tilde_in_fuzzy_with_empty_query_enters_navigation_at_home`, `slash_in_fuzzy_with_empty_query_enters_navigation_at_root` (+ variants) | shipped |
| AC-006 | Drill into a folder, then spawn at the chosen depth | T3 | `apps/tui/src/spawn.rs::tab_descends_into_folder_in_one_step`, `enter_with_selection_in_precise_descends`, `enter_without_selection_in_precise_spawns` | shipped |
| AC-007 | Saved project alias resolves through the minibuffer | T3 | `apps/tui/src/spawn.rs::build_project_meta_*`, `host_bound_tilde_project_round_trips_as_literal_path` | partial |
| AC-008 | Cancel the spawn minibuffer | T3 | `apps/tui/tests/pty_spawn.rs::esc_in_spawn_modal_closes_the_modal` (+ `spawn.rs::esc_in_nav_mode_with_no_selection_closes_modal`, `esc_in_search_mode_clears_filter_chars`, `esc_in_nav_mode_with_selection_clears_selection`) | shipped |
| AC-009 | Cycle focus between agents | T3 | `apps/tui/tests/pty_focus.rs::prefix_n_cycles_focus_between_two_agents` (+ `runtime.rs::prefix_l_via_alias_focuses_next`, `direct_cmd_apostrophe_focuses_next_without_arming_prefix`, `prefix_then_repeated_nav_keys_keeps_dispatching`) | shipped |
| AC-010 | Focus an agent by ordinal digit | T3 | `apps/tui/src/runtime.rs::prefix_digit_focuses_by_one_indexed_position`, `prefix_then_digit_stays_sticky`, `prefix_zero_is_consumed_no_focus` | partial |
| AC-011 | Bounce to the previously-focused agent | T3 | `apps/tui/tests/pty_bounce.rs::prefix_tab_bounces_focus_between_two_agents` (+ `runtime.rs::prefix_tab_dispatches_focus_last`, `change_focus_lets_alt_tab_bounce_via_two_calls`) | shipped |
| AC-012 | Switcher popup picks an agent by name | T3 | `apps/tui/src/runtime.rs::snapshot_navigator_popup`, `dismiss_clamps_open_popup_selection` (+ label-spans tests) | partial |
| AC-013 | Toggle the navigator chrome | T3 | `apps/tui/tests/pty_nav.rs::chrome_flips_from_popup_to_leftpane_on_prefix_v` | shipped |
| AC-014 | Force-close a live agent | T3 | `apps/tui/tests/pty_lifecycle.rs::kill_last_agent_auto_exits_codemux` (+ `runtime.rs::kill_focused_removes_ready_agent`, `kill_focused_clamps_focus_when_killing_last_tab`, `prefix_x_dispatches_kill_agent`) | shipped |
| AC-015 | Dismiss a crashed or failed agent (no-op on live) | T3 | `apps/tui/src/runtime.rs::dismiss_no_op_on_focused_ready_agent`, `dismiss_removes_focused_failed_agent`, `dismiss_removes_focused_crashed_agent_and_clamps_focus`, `prefix_d_dispatches_dismiss_agent` | shipped |
| AC-016 | Quit codemux cleanly | T3 | `apps/tui/tests/pty_lifecycle.rs::prefix_q_quits_codemux_cleanly` (+ `runtime.rs::prefix_q_exits`) | shipped |
| AC-017 | Enter scroll mode and navigate history | T3 | `apps/tui/src/runtime.rs::scrollback_zero_len_means_no_history`, `nudge_scrollback_*`, `jump_to_top_*`, `snap_to_live_*` | partial |
| AC-018 | Typing snaps to live; navigation preserves scroll | T3 | `apps/tui/src/runtime.rs::snap_to_live_resets_offset_to_zero`, `nudge_scrollback_only_touches_focused_agent`, `scrollback_state_is_per_parser` | partial |
| AC-019 | Click a tab to focus it | T3 | `apps/tui/src/runtime.rs::tab_mouse_dispatch_up_same_tab_is_a_click`, `tab_hitboxes_at_finds_recorded_rect` (+ hitbox tests) | shipped |
| AC-020 | Drag a tab to reorder | T3 | `apps/tui/src/runtime.rs::tab_mouse_dispatch_up_different_tab_is_a_reorder`, `reorder_agents_drag_right_inserts_at_destination`, `reorder_followed_by_shift_index_keeps_focus_on_the_moved_agent` | shipped |
| AC-021 | Drag-to-select and copy via OSC 52 | deferred | `apps/tui/src/runtime.rs::pane_mouse_dispatch_up_with_selection_commits`, `write_clipboard_to_emits_osc_52_with_base64_payload` (+ extraction tests) | partial — drag/select/extract/encode pinned at unit level; the original deferral concerns end-to-end OSC 52 round-trip via a real terminal, which still needs a clipboard sink. |
| AC-022 | Configured segments render in order | T1 | `apps/tui/src/status_bar/mod.rs::build_segments_default_set_is_model_tokens_worktree_branch_hint`, `render_segments_renders_all_when_space_is_ample` (+ per-segment tests) | shipped |
| AC-023 | Customize the status bar via config | T1 | `apps/tui/src/status_bar/mod.rs::build_segments_skips_unknown_ids_and_warns`, `apps/tui/src/config.rs::ui_status_bar_segments_round_trips_user_override`, `ui_status_bar_segments_empty_list_disables_right_side_block` | shipped |
| AC-024 | Segments drop from the left under width pressure | T1 | `apps/tui/src/status_bar/mod.rs::render_segments_drops_leftmost_first_when_space_is_tight`, `render_segments_keeps_only_the_rightmost_when_space_is_very_tight` | shipped |
| AC-025 | Help screen reflects the live keymap | T1 | `apps/tui/tests/pty_help.rs::help_overlay_opens_and_closes_on_chord` (+ `runtime.rs::snapshot_help_screen`, `prefix_question_mark_opens_help`) | shipped |
| AC-027 | Daemon serves a screen-state snapshot on every attach | T4 | `apps/daemon/src/supervisor.rs::snapshot_replays_screen_state_on_reattach`, `snapshot_drain_avoids_duplicate_replay_of_buffered_bytes`, `apps/daemon/src/session.rs::take_snapshot_alt_screen_includes_alt_screen_toggle` | shipped |
| AC-028 | Reattach to a remote agent across TUI restart | deferred | `daemon_proto::reattach_after_tui_restart_serves_snapshot` | deferred — blocked on AD-7 (persistence) and agent-id stability; AC body marks it "Not currently implementable." |
| AC-029 | Missing config file falls back to defaults | unit | `apps/tui/src/config.rs::empty_toml_yields_default_config`, `errors_when_neither_xdg_nor_home_is_set`, `apps/tui/src/keymap.rs::missing_config_returns_default_bindings` | shipped |
| AC-030 | Invalid config fails loud before raw mode | unit | `apps/tui/src/config.rs::invalid_chord_in_config_propagates_as_a_parse_error`, `host_colors_unknown_name_is_an_error`, `spawn_unknown_default_mode_is_an_error` | partial |
| AC-031 | Invalid `[PATH]` arg fails loud before raw mode | unit | `apps/tui/src/main.rs::resolve_cwd_errors_when_path_is_missing`, `resolve_cwd_errors_when_path_is_a_file` | partial |
| AC-032 | Spawn modal opens at the TUI startup cwd, not the focused agent's cwd | T3 | `apps/tui/src/spawn.rs::open_seeds_path_with_cwd_and_marks_auto_seeded` | partial |
| AC-033 | Spawn modal swallows all keystrokes while open | T3 | `apps/tui/src/spawn.rs::ctrl_modified_keys_are_dropped`, `lock_for_bootstrap_drops_typing_keys` | partial |
| AC-034 | Spawning a new agent records the prior focus as the bounce slot | T3 | `apps/tui/src/runtime.rs::change_focus_records_previous_when_focus_moves`, `change_focus_lets_alt_tab_bounce_via_two_calls` | partial |
| AC-035 | Reaping the focused agent moves focus to the new tail, not to `previous_focused` | T3 | `apps/tui/src/runtime.rs::kill_focused_clamps_focus_when_killing_last_tab`, `dismiss_removes_focused_crashed_agent_and_clamps_focus`, `dismiss_clears_previous_focused_when_it_collides_with_focused` | shipped |
| AC-036 | Reaping the last agent auto-exits codemux | T3 | `apps/tui/tests/pty_lifecycle.rs::kill_last_agent_auto_exits_codemux` (+ `runtime.rs::reap_silently_removes_clean_exit`, `dismiss_leaves_empty_vec_when_last_agent_dismissed`) | shipped |
| AC-037 | A non-zero PTY exit transitions Ready → Crashed (not silent removal) | T3 | `apps/tui/src/runtime.rs::reap_transitions_ready_with_dead_transport_to_crashed`, `mark_crashed_transitions_ready_to_crashed_preserving_parser_and_exit_code`, `render_agent_pane_paints_red_banner_for_nonzero_exit_code` | shipped |
| AC-038 | A panic restores the terminal before the report is printed | deferred | `pty_panic::panic_drops_terminal_guard_before_report` | deferred — no clean way to inject a panic into a running codemux from outside; would need a `--panic-after=<ms>` test seam. The `TerminalGuard` drop ordering itself is unit-testable separately. |
| AC-039 | Pasting while scrolled-back snaps to live before the bracketed-paste write | T3 | `apps/tui/src/runtime.rs::snap_to_live_resets_offset_to_zero`, `wrap_paste_emits_brackets_around_plain_text` | partial |
| AC-040 | Mouse events are suppressed while an overlay is open | T3 | `apps/tui/src/runtime.rs::no_overlay_active_returns_false_when_help_open`, `no_overlay_active_returns_false_when_popup_open` | partial |
| AC-041 | Ctrl+click on a URL hands it to the OS opener; Ctrl+hover shows underline + hand cursor | deferred | `apps/tui/src/runtime.rs::compute_hover_returns_url_under_pane_cell`, `url_opener_trait_supports_recording_mock_implementations`, `paint_hover_url_if_active_underlines_url_range_and_tints_cyan` (+ url_scan tests) | partial — detection / hover state / opener trait / toast wiring all pinned at unit level; the original deferral concerns end-to-end Ctrl+click against a real OS opener, which still needs an integration seam. |
| AC-042 | Status bar is hidden in `LeftPane` chrome | T1 | `render_status_bar::leftpane_hides_status_bar` | planned |
| AC-043 | Daemon survives SSH disconnect via `setsid -f` | T4 | `crates/codemuxd-bootstrap/src/lib.rs::spawn_remote_daemon_redirects_stdin_to_devnull_and_stderr_to_sibling_file` | partial *(uncertain — see notes)* |
| AC-044 | Stale daemon is killed and re-deployed on local-binary upgrade | T4 | `crates/codemuxd-bootstrap/src/lib.rs::spawn_remote_daemon_runs_kill_prelude_when_force_respawn`, `spawn_remote_daemon_omits_kill_prelude_when_not_force_respawn`, `prepare_remote_skips_install_when_version_matches` | partial |
| AC-045 | Indexing runs in the background; input stays interactive | T3 | `apps/tui/src/index_manager.rs::drain_*`, `apps/tui/src/fuzzy_worker.rs::*`, `apps/tui/src/spawn.rs::ctrl_t_toggles_fuzzy_to_precise_and_seeds_cwd`, `ctrl_r_emits_refresh_index_outcome_in_fuzzy_mode` | partial |

Notes on uncertain rows:

- **AC-043** is tagged T4 because it's a daemon-survival assertion, but exercising the real failure mode (kill the SSH ControlMaster, observe daemon still running) is awkward without a real sshd. May end up split into a T4 "daemon process group is detached" check plus a manual integration-only step. Re-decide when the test is written.
