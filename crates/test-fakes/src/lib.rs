//! Test-fake binaries shared by the slow-tier PTY/proto E2E suites.
//! No public Rust API — see `src/bin/` for the executables and the
//! consuming test crates' `common/mod.rs` for invocation paths.
